{
	"name": "@repo/env",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"description": "Validated server-side environment. Import `env` instead of touching process.env.",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@t3-oss/env-core": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@repo/tsconfig": "workspace:*",
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	}
}
