# Files that run on Linux servers must keep LF endings whatever the contributor's platform
# (core.autocrlf on Windows would otherwise hand `bash` a "\r" after every line).
*.sh text eol=lf
*.Caddyfile text eol=lf
Dockerfile* text eol=lf
